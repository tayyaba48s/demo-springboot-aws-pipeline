version: 0.2
phases:
  install:
    commands:
      - java -version
  build:
    commands:
      - echo Packaging JAR file...
      - mvn clean package
  post_build:
    commands:
      - echo Build complete.
artifacts:
  files:
    - target/demo.jar
  discard-paths: yes